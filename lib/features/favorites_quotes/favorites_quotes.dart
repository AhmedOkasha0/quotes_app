import 'package:flutter/material.dart';

class FavouriteQuote extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Column(
        children: [
          Text('Favourite Qoutes'),
        ],
      ),
    );
  }
}
